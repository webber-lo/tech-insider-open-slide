import type { SlideMeta } from '@open-slide/core';
import { deckMeta } from './content';
import { channelPlanSlides } from './slides';
import { design } from './theme';

export { design };

export const meta: SlideMeta = deckMeta;

export default channelPlanSlides;
